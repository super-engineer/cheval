<%= form_for @print_medium, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="clearfix">
    <div class="pull-left">
      <div class="control-group">
        <%= f.label :hidden, :class => 'control-label' %>
        <div class="controls">
          <%= f.check_box :hidden, :class => 'check_box' %>
        </div>
      </div>
      <div class="control-group">
        <%= f.label :image, "#{@print_medium.persisted? ? 'Change Image' : 'Image' }", :class => 'control-label' %>
        <div class="controls">
          <%= f.file_field :image, :class => 'file_field' %>
        </div>
      </div>
    </div>
    <div class="pull-left">
      <%= image_tag @print_medium.image.url(:thumb) if @print_medium.persisted? %>
    </div>
  </div>
  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-success' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                print_media_path, :class => 'btn' %>
  </div>
<% end %>