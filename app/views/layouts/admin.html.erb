<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Cheval" %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "admin", :media => "all" %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

  </head>
  <body>
    <div class="wrap">
      <div class="navbar navbar-fixed-top">
        <div class="navbar-inner">
          <div class="container">
            <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </a>
            <a class="brand" href="<%= root_path %>">Cheval</a>
            <div class="container nav-collapse">
              <ul class="nav">
                <li><%= link_to "Events", events_path %></li>
                <li><%= link_to "Home Slider", slider_images_path %></li>
                <li><%= link_to "Special Menus", special_menus_path %></li>
                <li><%= link_to "Press Links", press_links_path %></li>
              </ul>
              <ul class="pull-right nav">
                <% if admin_signed_in? %>
                  <li class="dropdown">
                      <%= link_to current_admin.email, "javascript:void(0)", :method => :delete, "data-toggle" => "dropdown", :class => "dropdown-toggle" %>
                      <ul class="dropdown-menu" role="menu" aria-labelledby="drop1">
                        <li><%= link_to "Settings", edit_admin_registration_path %></li>
                        <li><%= link_to "Logout", destroy_admin_session_path, :method => :delete %></li>
                      </ul>
                    </li>
                <% end %>
                
              </ul>
            </div><!--/.nav-collapse -->
          </div>
        </div>
      </div>

      <div class="clearfix"></div>
      <div class="container main">
        <div class="row">
          <div class="span12">
            <%= yield %>
          </div>
        </div>
      </div>
      <div id="push"></div>
    </div>
    <div id="footer">
      
    </div>
    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <%= js extend_gritter :position => :bottom_left %>
    <%= tinymce %>
    <script type="text/javascript">
      <% if notice %>
        <%= add_gritter(notice, :image => :success, :title => "Success!!!") %>
      <% end %>
      <% if alert %>
        <%= add_gritter(alert, :image => :error, :title => "Oops!!!") %>
      <% end %>
    </script>
    <%= gflash %>
  </body>
</html>
